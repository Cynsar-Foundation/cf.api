'use strict';

/**
 * spinning-of-yarn controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::spinning-of-yarn.spinning-of-yarn');
