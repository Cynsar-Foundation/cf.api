'use strict';

/**
 * weaving-of-fabric controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::weaving-of-fabric.weaving-of-fabric');
